# Copy to `.env.local` for development, or set in Netlify under
# Site configuration -> Environment variables.

# n8n production webhook that receives contact form submissions.
# See automation/README.md. Server-side only; never exposed to the browser.
N8N_CONTACT_WEBHOOK_URL=
